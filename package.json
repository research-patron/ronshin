{
  "name": "research-news-app",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev": "cd research-news-app && npm run dev",
    "build": "cd research-news-app && npm install && npm run build",
    "start": "cd research-news-app && npm start",
    "postinstall": "cd research-news-app && npm install"
  }
}